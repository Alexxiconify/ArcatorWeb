# Node modules
node_modules/
# IDE files
.idea/
.vscode/
.cursor/
# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.git/

# Development files and tools
dev/

# Sensitive files and folders
sensitive/

# EmailJS credentials and sensitive data
emailjs_credentials.json
.env
.env.local
.env.production
.env.development

# Firebase config files with sensitive data
firebase-config.json
firebase-keys.json

# SSH Keys and sensitive server files
apollo_key
apollo_key.pub
*.pem
*.key
*.crt
*.p12
*.pfx

# SMTP Server configuration
smtp-server/.env
smtp-server/config.json
smtp-server/credentials.json

# Deployment scripts with sensitive data
deploy-smtp-apollo.ps1
setup-smtp-apollo.sh

# Log files
*.log
pglite-debug.log

# Temporary files
*.tmp
*.temp
temp-*

# Environment files
.env*
!.env.example

# API Keys and secrets
*api-key*
*secret*
*token*
*password*
*credential*

# Functions node_modules (if not using npm workspaces)
functions/node_modules/